/*import { BucketProduct } from './bucket-product';
import { ClientProduct } from './client-product';
import { Product } from './product';

describe('BucketProduct', () => {
  it('should create an instance', () => {
    const product = new ClientProduct("ELDERWATER WAND", "DO MAGIC TRICK", 5, "EW", 15, [], false, 150, []);
    const bucketProduct = new BucketProduct(product, product.price*(product.reductionRate/100));
    expect(bucketProduct).toBeTruthy();
  });
});
*/